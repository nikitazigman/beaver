[tool.poetry]
name = "src"
version = "0.1.0"
authors = ["Pavel Eroshkin <eroshkin321@gmail.com>"]
readme = "README.md"
documentation = "https://www.geeksforgeeks.org/bin-packing-problem-minimize-number-of-used-bins/"
description = "Bin Packing Algorithms: Next Fit, First Fit, and Best Fit"
keywords = ["bin packing", "next fit", "first fit", "best fit", "data structures"]

[tool.poetry.dependencies]
python = "^3.12"
pytest = "^7.4.3"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
